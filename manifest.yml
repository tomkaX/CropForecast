---
declared-services:
  mean-mongo:
    label: mongolab
    plan: sandbox
applications:
- name: cf
  host: cf-${random-word}
  memory: 128M
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
